# Deprecated Duke splash assets

These Duke Nukem splash images are retained only as historical reference
assets. They are not used by the active UDK editor, game, or PC-console launch
paths. The active splash files under `Engine\Splash` use the supplied neutral
UDK artwork instead.
